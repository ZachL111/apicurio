# Golden Cases

The golden set keeps `contract drift` and `breaking risk` visible as named cases.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `contract drift`, `schema width`, `breaking risk`, and `consumer reach` with different score ranges.

The purpose is to make intentional rule changes show up in review.
